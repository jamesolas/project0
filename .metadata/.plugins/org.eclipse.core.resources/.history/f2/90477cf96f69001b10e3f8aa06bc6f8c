package com.app.service.impl;

import static org.junit.jupiter.api.Assertions.*;

import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;

import com.app.dao.impl.UserDAOImpl;
import com.app.model.User;
import com.app.service.UserService;

public class UserServiceImplTest {
	
	@InjectMocks
	private static UserServiceImpl userServiceImpl;
	
	@Mock
	private UserDAOImpl userDAOImpl;

	@BeforeAll
	public static void setUpBeforeClass() {
		userServiceImpl = new UserServiceImpl();
	}

	@BeforeEach
	void setUp() throws Exception {
	}

	@Test
	void testCreateAccount() {
		List<User> userList = new ArrayList<>();
		String firstName = "testFirst";
		String lastName = "testLast";
		String email = "testEmail";
		String password = "testPassword";
		
		User user = new User(firstName, lastName, email, password);
		//user = (firstName, lastName, email, password);
		String testString = ("[testFirst, testLast, testEmail, testPassword]");
		
		Mockito.when(userDAOImpl.accountCreation(user)).thenReturn(testString);
		assertEquals(testString.compareTo(userDAOImpl.accountCreation(user)));
		
		assertTrue(user.compareTo(userDAOImpl.accountCreation(user)));
	}

	@Test
	void testLogIn() {
		fail("Not yet implemented");
	}

}
